{
  "name": "resto",
  "version": "1.0.0",
  "description": "Modular business landing page with dashboard and SQLite backend",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "migrate": "node -e \"const setup = require('./src/db/setup'); setup.runMigrations(); setup.seedDefaults(); console.log('Database ready');\"",
    "test": "node -e \"console.log('No automated tests specified');\""
  },
  "keywords": ["landing-page", "dashboard", "sqlite", "express"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
